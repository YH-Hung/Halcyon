# Halcyon clang-tidy configuration.
#
# Conservative, bug-focused linting for Halcyon's own code. WarningsAsErrors is
# intentionally empty so newer clang-tidy releases can be evaluated before they
# become gating.
Checks: >
  bugprone-*,
  -bugprone-easily-swappable-parameters,
  performance-*,
  -performance-enum-size,
  portability-*,
  -portability-avoid-pragma-once,
  google-explicit-constructor,
  modernize-use-override,
  modernize-use-nullptr,
  modernize-use-equals-default,
  modernize-use-using,
  readability-misleading-indentation,
  readability-non-const-parameter,
  readability-redundant-member-init,
  readability-redundant-string-init
WarningsAsErrors: ''
# Only lint Halcyon's own headers; skip the vendored driver and GoogleTest.
HeaderFilterRegex: 'include/halcyon/'
FormatStyle: file
